{
  "name": "@freehub/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --emitDeclarationOnly && esbuild src/index.ts --bundle --format=esm --platform=neutral --outfile=dist/index.js",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
